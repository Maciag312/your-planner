import React from 'react'

export default function TaskTimerView() { //It's dummy component that app routs too, it has no body because TaskTimer has View but only shows itself when route is on in order to make functions run in the background
    return (
        <div>
        </div>
    )
}
